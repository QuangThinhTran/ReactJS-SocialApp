export enum auth {
    LOGIN = 'auth/login',
    REGISTER = 'auth/register',
    LOGOUT = 'auth/logout',
}

export enum blog {
    CREATE = 'blog/create',
    GET = 'auth/register',
    LOGOUT = 'auth/logout',
}