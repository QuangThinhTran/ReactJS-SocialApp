export enum AUTH {
    LOGIN = 'auth/login',
    REGISTER = 'auth/register',
    LOGOUT = 'auth/logout',
}

export enum BLOG {
    GET = 'blogs',
    CREATE = 'blog/create',
    DETAIL = 'blog/detail',
    DELETE = 'blog/delete',
}

export enum USER {
    GET = 'users',
    DETAIL = 'user/',
    UPDATE = 'user/update',
}